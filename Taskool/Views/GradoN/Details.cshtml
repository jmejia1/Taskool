@model Taskool.Models.GradoN

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>GradoN</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.nombreGradoN)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.nombreGradoN)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
