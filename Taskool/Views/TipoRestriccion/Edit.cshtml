@model Taskool.Models.Tipo_Restriccion

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Tipo_Restriccion</legend>

        @Html.HiddenFor(model => model.id)

        <div class="editor-label">
            @Html.LabelFor(model => model.nombreTipoRestriccion)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.nombreTipoRestriccion)
            @Html.ValidationMessageFor(model => model.nombreTipoRestriccion)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
